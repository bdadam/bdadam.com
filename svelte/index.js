// const fs = require('fs');

// const svelte = require('svelte/compiler');

// const source = fs.readFileSync('svelte/App.svelte', 'utf-8');

// const result = svelte.compile(source, {
//     generate: 'ssr',
// });

// console.log(result);
