name: Healthcheck
on:
  schedule:
    - cron: '18 13,14,15 * * *'

jobs:
  healthcheck:
    runs-on: ubuntu-22.04
    steps:
      - run: |
          curl -sSf https://bdadam.com/ > /dev/null
          curl -sSf https://expired.badssl.com/ > /dev/null
