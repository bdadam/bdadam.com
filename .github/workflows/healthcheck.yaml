name: Healthcheck
on:
  schedule:
    - cron: '38,40,42,45 10-15 * * *'

jobs:
  healthcheck:
    runs-on: ubuntu-22.04
    steps:
      - run: |
          curl -sSf https://bdadam.com/ > /dev/null
          curl -sSf https://expired.badssl.com/ > /dev/null
